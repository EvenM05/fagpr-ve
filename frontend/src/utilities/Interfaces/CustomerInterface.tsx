export interface CustomerData {
  id: string;
  name: string;
  contactMail: string;
  organizationNumber: number;
}

export interface CreateCustomerData {
  name: string;
  contactMail: string;
  organizationNumber: number;
}
