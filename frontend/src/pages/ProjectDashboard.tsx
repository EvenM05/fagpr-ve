import { Box, Typography } from "@mui/material";

export default function ProjectDashboard() {
  return (
    <Box>
      <Typography sx={{ color: "black" }}>dashboard black</Typography>
      <Typography sx={{ color: "white" }}>dashboard white</Typography>
    </Box>
  );
}
